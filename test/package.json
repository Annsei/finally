{
  "name": "finally-e2e",
  "version": "0.1.0",
  "private": true,
  "description": "Playwright E2E tests for FinAlly (PLAN.md §12)",
  "scripts": {
    "test": "playwright test",
    "test:headed": "playwright test --headed",
    "report": "playwright show-report"
  },
  "devDependencies": {
    "@playwright/test": "1.61.1"
  }
}
