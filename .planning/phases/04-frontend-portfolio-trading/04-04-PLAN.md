---
phase: 04-frontend-portfolio-trading
plan: 04
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/__tests__/TradeBar.test.tsx
  - frontend/src/components/TradeBar.tsx
autonomous: true
requirements: [FE-13]
must_haves:
  truths:
    - "D-13: The trade bar submits a market buy or sell to POST /api/portfolio/trade and optimistically updates the portfolio"
    - "D-12: Clicking a watchlist ticker auto-fills the trade bar ticker input"
    - "Invalid ticker/quantity is rejected client-side before any network call"
    - "D-14: Trade validation errors (e.g. insufficient cash) display inline below the inputs and clear on the next attempt"
  artifacts:
    - path: "frontend/src/components/TradeBar.tsx"
      provides: "Trade form with optimistic SWR mutate, input validation, inline errors"
      contains: "optimisticData"
      min_lines: 40
  key_links:
    - from: "frontend/src/components/TradeBar.tsx"
      to: "/api/portfolio/trade"
      via: "fetch POST inside SWR mutate"
      pattern: "api/portfolio/trade"
    - from: "frontend/src/components/TradeBar.tsx"
      to: "/api/portfolio cache"
      via: "useSWR + mutate (shared key with Header)"
      pattern: "'/api/portfolio'"
---

<objective>
Build the TradeBar (FE-13): ticker input (auto-filled from selectedTicker), quantity input, Buy (green) and Sell (red) buttons. Executes instant market orders via POST /api/portfolio/trade with an optimistic SWR update, client-side input validation, and inline error display.

Purpose: This is the manual trading surface. It owns its own file and shares the `/api/portfolio` SWR key with the Header and portfolio panels so a trade updates everything at once.
Output: TradeBar.tsx and its Wave 0 test file (RED-first).
</objective>

<execution_context>
@/Users/maoqin/learning/ai-coder/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maoqin/learning/ai-coder/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-frontend-portfolio-trading/04-RESEARCH.md
@.planning/phases/04-frontend-portfolio-trading/04-PATTERNS.md
@.planning/phases/04-frontend-portfolio-trading/04-UI-SPEC.md

<interfaces>
<!-- From frontend/src/types/market.ts -->
export interface PortfolioResponse { cash: number; total_value: number; positions: Position[]; }

<!-- Backend POST /api/portfolio/trade (verified portfolio.py) -->
Request:  { ticker: string, quantity: number, side: "buy" | "sell" }
Success(200): { status: "ok", ticker, side, quantity, price, trade_id }
Error(400):   { error: "Insufficient cash" }

<!-- SWR v2 mutate optimistic signature (installed swr@2.4.1) -->
mutate(asyncMutator, { optimisticData, rollbackOnError: true, revalidate: true })

<!-- From frontend/src/stores/priceStore.ts -->
usePriceStore.getState().prices[ticker]?.price  // fallback price source
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: TradeBar component (FE-13) + test</name>
  <files>frontend/__tests__/TradeBar.test.tsx, frontend/src/components/TradeBar.tsx</files>
  <read_first>
    - frontend/src/components/Header.tsx (useSWR('/api/portfolio', fetcher) + mutate binding — the SWR key MUST match exactly so the trade revalidates the Header)
    - frontend/__tests__/WatchlistRow.test.tsx (render + event + state assertion patterns for component tests)
    - .planning/phases/04-frontend-portfolio-trading/04-PATTERNS.md (TradeBar section — exact optimistic mutate, validation, auto-fill, SWR mock patterns)
    - .planning/phases/04-frontend-portfolio-trading/04-UI-SPEC.md (trade button colors: Buy green #22c55e, Sell red #ef4444; error copy strings; input labels Ticker/Qty)
  </read_first>
  <behavior>
    - Test (T-4-01/T-4-03 security): submitting an empty/invalid ticker or a quantity <= 0 / non-finite sets an inline error and does NOT call fetch
    - Test: clicking Buy with valid inputs calls fetch with POST /api/portfolio/trade and body { ticker, quantity, side: "buy" }
    - Test: a 400 response ({ error: "Insufficient cash" }) shows an inline error message; submitting again first clears the prior error
    - Test: when selectedTicker prop changes, the ticker input value syncs to it
  </behavior>
  <action>
    Write frontend/__tests__/TradeBar.test.tsx FIRST (RED). Mock global.fetch (jest.fn) and mock 'swr' default export to return `{ data: mockPortfolio, mutate: mockMutate }` where mockMutate invokes the async mutator (and respects rollbackOnError by swallowing the throw). Then implement frontend/src/components/TradeBar.tsx as default export taking `{ selectedTicker: string | null; onTradeComplete?: () => void }`. State: `ticker` (string), `qty` (string), `error: string | null`, `pending: boolean`. Bind SWR: `const { data: portfolio, mutate } = useSWR<PortfolioResponse>('/api/portfolio', fetcher)` — exact key, no trailing slash. Auto-fill: `useEffect(() => { if (selectedTicker) setTicker(selectedTicker); }, [selectedTicker])`. Validation BEFORE any network call (security T-4-01/T-4-03): `const trimmedTicker = ticker.trim().toUpperCase(); if (!trimmedTicker || !/^[A-Z]+$/.test(trimmedTicker)) { setError('Enter a valid ticker and quantity.'); return; }` and `if (!isFinite(Number(qty)) || Number(qty) <= 0) { setError('Enter a valid ticker and quantity.'); return; }`. handleTrade(side): clear error, set pending, then `await mutate(asyncMutator, { optimisticData, rollbackOnError: true, revalidate: true })`. asyncMutator does `fetch('/api/portfolio/trade', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify({ ticker: trimmedTicker, quantity: Number(qty), side }) })`; on `!res.ok` read json and `throw new Error(data.error ?? 'Trade failed')`; on ok return current. optimisticData computes price from `current.positions.find(p => p.ticker === trimmedTicker)?.current_price ?? usePriceStore.getState().prices[trimmedTicker]?.price ?? 0` and returns `{ ...current, cash: current.cash + (side === 'sell' ? cost : -cost) }`. Wrap in try/catch to `setError(e.message)`, finally `setPending(false)`. UI: a `Ticker` text input and `Qty` number input (12px sans labels), a green Buy button (bg using terminal-up #22c55e), a red Sell button (terminal-down #ef4444), both disabled while pending. Inline error rendered below inputs in red 12px when error is set. Use error copy from UI-SPEC: insufficient-cash errors come straight from the API message; invalid-input uses `Enter a valid ticker and quantity.`
  </action>
  <verify>
    <automated>cd frontend && npm test -- --testPathPattern=TradeBar --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/TradeBar.tsx contains `'/api/portfolio/trade'` and `method: 'POST'`
    - frontend/src/components/TradeBar.tsx contains `optimisticData` and `rollbackOnError: true`
    - frontend/src/components/TradeBar.tsx contains `.trim().toUpperCase()` and `isFinite(` validation guards
    - frontend/src/components/TradeBar.tsx contains an `useEffect` syncing the ticker input to `selectedTicker`
    - frontend/__tests__/TradeBar.test.tsx asserts: invalid input blocks fetch, Buy click POSTs with side "buy", 400 shows inline error
    - `npm test -- --testPathPattern=TradeBar --watchAll=false` exits 0
  </acceptance_criteria>
  <done>TradeBar validates input, posts buy/sell orders with optimistic update, auto-fills from selection, and surfaces inline errors; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → POST /api/portfolio/trade | User-supplied ticker + quantity cross to the backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-01 | Tampering | ticker input | mitigate | Trim + uppercase + `/^[A-Z]+$/` validation before submit; backend validates ticker against price cache independently |
| T-4-03 | Denial of Service | quantity input | mitigate | `isFinite(Number(qty)) && Number(qty) > 0` validated client-side before POST; backend validates independently |
| T-4-OPT | Tampering | optimistic cash state | mitigate | `rollbackOnError: true` on the SWR mutate reverts optimistic state when the trade fails; `revalidate: true` reconciles with server truth |
| T-4-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — swr already installed and approved |
</threat_model>

<verification>
- `cd frontend && npm test -- --testPathPattern=TradeBar --watchAll=false` passes
</verification>

<success_criteria>
- TradeBar executes validated buy/sell market orders with optimistic update + inline errors (FE-13)
</success_criteria>

<output>
Create `.planning/phases/04-frontend-portfolio-trading/04-04-SUMMARY.md` when done
</output>
