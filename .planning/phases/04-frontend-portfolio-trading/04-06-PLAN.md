---
phase: 04-frontend-portfolio-trading
plan: 06
type: execute
wave: 3
depends_on: ["04-02", "04-03", "04-04", "04-05"]
files_modified:
  - frontend/src/pages/index.tsx
  - frontend/__tests__/index.test.tsx
autonomous: false
requirements: [FE-09, FE-10, FE-11, FE-12, FE-13, FE-14, FE-15]
must_haves:
  truths:
    - "The dashboard renders the 3-column layout: watchlist | center content | collapsible chat"
    - "On load with no selection, the first watchlist ticker is auto-selected so the main chart is never blank"
    - "The center column stacks main chart → [heatmap | P&L] → trade bar → positions table"
    - "The chat panel is open by default and collapses via its toggle"
  artifacts:
    - path: "frontend/src/pages/index.tsx"
      provides: "3-column dashboard wiring all Phase 4 panels"
      contains: "MainChart"
      min_lines: 40
  key_links:
    - from: "frontend/src/pages/index.tsx"
      to: "all Phase 4 components"
      via: "imports + layout composition"
      pattern: "import MainChart"
    - from: "frontend/src/pages/index.tsx"
      to: "selectedTicker auto-select"
      via: "useSWR /api/watchlist + useEffect"
      pattern: "setSelectedTicker"
---

<objective>
Wire all six Phase 4 components into the existing `index.tsx` 3-column dashboard, replacing the Phase 3 placeholder comment. Add the chat-open state, the auto-select-first-ticker effect, and the center-column stack. Add an index render test.

Purpose: This is the integration plan (Wave 3). It depends on every component plan and composes them into the final layout per CONTEXT D-01–D-04 and D-09.
Output: Updated index.tsx and index.test.tsx.
</objective>

<execution_context>
@/Users/maoqin/learning/ai-coder/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maoqin/learning/ai-coder/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-frontend-portfolio-trading/04-RESEARCH.md
@.planning/phases/04-frontend-portfolio-trading/04-PATTERNS.md
@.planning/phases/04-frontend-portfolio-trading/04-UI-SPEC.md

<interfaces>
<!-- Components built in Wave 2 (all default exports in frontend/src/components/) -->
MainChart        ({ ticker: string })
PnLChart         ()
PortfolioHeatmap ()
PositionsTable   ()
TradeBar         ({ selectedTicker: string | null; onTradeComplete?: () => void })
ChatPanel        ({ open: boolean; onToggle: () => void; onNewTrade?: () => void })

<!-- Current index.tsx (full file) — see read_first; placeholder comment to replace -->
<!-- From frontend/src/types/market.ts -->
export interface WatchlistResponse { tickers: WatchlistEntry[]; }
<!-- From frontend/src/lib/fetcher.ts -->
export const fetcher: (url: string) => Promise<any>;
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Wire 3-column dashboard in index.tsx + render test</name>
  <files>frontend/src/pages/index.tsx, frontend/__tests__/index.test.tsx</files>
  <read_first>
    - frontend/src/pages/index.tsx (full current file — Header + WatchlistPanel + selectedTicker state + the placeholder comment to replace; usePriceStream called once here)
    - frontend/__tests__/index.test.tsx (existing index render test — extend it; see how it mocks usePriceStream/SWR)
    - .planning/phases/04-frontend-portfolio-trading/04-RESEARCH.md (the "index.tsx Wiring" section — exact layout JSX, auto-select effect, height constraint)
    - .planning/phases/04-frontend-portfolio-trading/04-PATTERNS.md (index.tsx section — additions required list)
    - frontend/src/components/Header.tsx (useSWR('/api/portfolio') + mutate — reuse the bound mutate to pass onTradeComplete/onNewTrade revalidation)
  </read_first>
  <behavior>
    - Test: index renders without throwing and includes the three columns (watchlist present from Phase 3, center content, chat)
    - Test: MainChart, PortfolioHeatmap, PnLChart, TradeBar, PositionsTable, ChatPanel are all mounted
    - Test: when watchlist data has tickers and no ticker is selected, the first ticker is auto-selected (assert MainChart receives a non-empty ticker, or selection state effect runs)
  </behavior>
  <action>
    Extend frontend/__tests__/index.test.tsx FIRST (RED) to assert the new components mount and the layout has three columns — mock the six new components as lightweight stubs (or mock their heavy deps) and mock 'swr'/usePriceStream as the existing test does. Then modify frontend/src/pages/index.tsx: (1) import MainChart, PnLChart, PortfolioHeatmap, PositionsTable, TradeBar, ChatPanel from '@/components/...', plus `useEffect` from 'react', `useSWR` from 'swr', `fetcher` from '@/lib/fetcher', and `WatchlistResponse` type. (2) Add `const [chatOpen, setChatOpen] = useState(true)` (open by default — D-09). (3) Add `const { data: portfolio, mutate: mutatePortfolio } = useSWR<PortfolioResponse>('/api/portfolio', fetcher)` and `const { data: watchlistData } = useSWR<WatchlistResponse>('/api/watchlist', fetcher)`. (4) Auto-select first ticker (D-03): `useEffect(() => { if (!selectedTicker && watchlistData?.tickers?.length) setSelectedTicker(watchlistData.tickers[0].ticker); }, [watchlistData, selectedTicker])`. (5) Replace the placeholder comment with the 3-column layout: outer wrapper `flex gap-4 p-4 h-[calc(100vh-52px)] overflow-hidden`; column 1 is the existing WatchlistPanel; column 2 is `<div className="flex-1 flex flex-col gap-4 overflow-auto">` containing in order (D-02/D-04) `<MainChart ticker={selectedTicker ?? ''} />`, then `<div className="flex gap-4"><PortfolioHeatmap /><PnLChart /></div>`, then `<TradeBar selectedTicker={selectedTicker} onTradeComplete={() => mutatePortfolio()} />`, then `<PositionsTable />`; column 3 is `<div className={shrink-0 overflow-hidden transition-all duration-300 border-l border-terminal-border ${chatOpen ? 'w-80' : 'w-8'}}>` wrapping `<ChatPanel open={chatOpen} onToggle={() => setChatOpen(!chatOpen)} onNewTrade={() => mutatePortfolio()} />`. Keep `usePriceStream()` and `<Header />` exactly as they are. Do not introduce a second EventSource — usePriceStream stays the single source (anti-pattern guard).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --testPathPattern=index --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/pages/index.tsx contains `import MainChart`, `PnLChart`, `PortfolioHeatmap`, `PositionsTable`, `TradeBar`, `ChatPanel`
    - frontend/src/pages/index.tsx contains `const [chatOpen, setChatOpen] = useState(true)`
    - frontend/src/pages/index.tsx contains the auto-select `useEffect` calling `setSelectedTicker(watchlistData.tickers[0].ticker)`
    - frontend/src/pages/index.tsx contains `flex-1 flex flex-col` center column and `w-80`/`w-8` chat column toggle
    - frontend/src/pages/index.tsx still contains `usePriceStream()` exactly once and `<Header />`
    - frontend/__tests__/index.test.tsx asserts the three columns render and the new panels mount
    - `npm test -- --testPathPattern=index --watchAll=false` exits 0
  </acceptance_criteria>
  <done>index.tsx renders the full 3-column dashboard with all panels wired, auto-selects the first ticker, and its render test passes.</done>
</task>

<task type="checkpoint:human-verify" gate="blocking">
  <name>Task 2: Human verification of the live dashboard</name>
  <what-built>The complete Phase 4 dashboard: main chart, portfolio heatmap, P&L chart, positions table, trade bar, and AI chat panel wired into the 3-column layout. The full frontend test suite and backend chat tests pass.</what-built>
  <how-to-verify>
    1. Run the full automated suites first: `cd frontend && npm test -- --watchAll=false` (all green) and `cd backend && uv run --extra dev pytest -v` (all green).
    2. Build + run the app per the project start script (or `cd frontend && npm run build` then serve via FastAPI). Open http://localhost:8000.
    3. Confirm the 3-column layout: watchlist on the left, center content, chat panel on the right (open by default).
    4. Confirm the main chart auto-renders for the first watchlist ticker on load (never blank) and updates live; click another watchlist row and confirm the chart switches cleanly (no discontinuous jump).
    5. Confirm the positions table current-price column flashes green/red on price updates (after you own a position).
    6. Use the trade bar: buy a few shares; confirm cash decreases, a position appears, the heatmap tile renders sized/colored, and the positions table updates. Try an over-budget buy and confirm the inline error appears below the inputs.
    7. Confirm the P&L chart shows portfolio value over time (may need ~30s for a second snapshot).
    8. With LLM_MOCK=true, send a chat message; confirm the loading indicator appears, the assistant reply renders, and any trade/watchlist action shows as a badge below the bubble. Collapse and re-expand the chat panel via its toggle.
  </how-to-verify>
  <resume-signal>Type "approved" or describe any visual/functional issues to fix.</resume-signal>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| composition only | This plan wires existing components; trust boundaries are owned by the component plans (02–05) |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-ES | Tampering | EventSource duplication | mitigate | usePriceStream() stays the single SSE source in index.tsx; Phase 4 components consume useTicker only — no new EventSource (anti-pattern guard) |
| T-4-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced |
</threat_model>

<verification>
- `cd frontend && npm test -- --watchAll=false` (full suite) passes
- `cd backend && uv run --extra dev pytest -v` passes
- Human checkpoint approved
</verification>

<success_criteria>
- Full 3-column dashboard renders with all six panels wired (FE-09–FE-15)
- First ticker auto-selected on load (D-03); chat open by default and collapsible (D-09)
- Full frontend + backend suites green; human verification approved
</success_criteria>

<output>
Create `.planning/phases/04-frontend-portfolio-trading/04-06-SUMMARY.md` when done
</output>
