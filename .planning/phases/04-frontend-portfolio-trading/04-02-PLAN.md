---
phase: 04-frontend-portfolio-trading
plan: 02
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/__tests__/MainChart.test.tsx
  - frontend/src/components/MainChart.tsx
  - frontend/__tests__/PnLChart.test.tsx
  - frontend/src/components/PnLChart.tsx
autonomous: true
requirements: [FE-09, FE-11]
must_haves:
  truths:
    - "A larger price-over-time chart renders for the selected ticker and updates live from the SSE price store"
    - "Switching the selected ticker clears the previous ticker's line (no discontinuous jump)"
    - "A P&L area chart renders total portfolio value over time from GET /api/portfolio/history and re-polls every 30s"
  artifacts:
    - path: "frontend/src/components/MainChart.tsx"
      provides: "Selected-ticker line chart (Lightweight Charts)"
      contains: "createChart"
      min_lines: 30
    - path: "frontend/src/components/PnLChart.tsx"
      provides: "Portfolio value area chart with 30s SWR poll"
      contains: "AreaSeries"
      min_lines: 30
  key_links:
    - from: "frontend/src/components/MainChart.tsx"
      to: "priceStore useTicker"
      via: "useTicker(ticker) selector"
      pattern: "useTicker\\("
    - from: "frontend/src/components/PnLChart.tsx"
      to: "/api/portfolio/history"
      via: "useSWR with refreshInterval 30000"
      pattern: "api/portfolio/history"
---

<objective>
Build the two Lightweight Charts canvas components: MainChart (FE-09 — selected ticker, live from SSE) and PnLChart (FE-11 — portfolio value over time, 30s poll). Both follow the established SparklineChart.tsx instance lifecycle.

Purpose: These are the chart panels for the center column. They share an identical chart-instance pattern, so they are planned together.
Output: MainChart.tsx, PnLChart.tsx, and their Wave 0 test files (RED-first).
</objective>

<execution_context>
@/Users/maoqin/learning/ai-coder/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maoqin/learning/ai-coder/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-frontend-portfolio-trading/04-RESEARCH.md
@.planning/phases/04-frontend-portfolio-trading/04-PATTERNS.md
@.planning/phases/04-frontend-portfolio-trading/04-UI-SPEC.md

<interfaces>
<!-- From frontend/src/stores/priceStore.ts -->
export const useTicker = (ticker: string) => PriceUpdate | undefined;
<!-- PriceUpdate: { ticker, price, previous_price, timestamp, change, change_percent, direction } -->

<!-- From frontend/src/types/market.ts (added in Plan 01) -->
export interface PortfolioHistoryResponse { snapshots: { total_value: number; recorded_at: string }[]; }

<!-- From frontend/src/lib/fetcher.ts -->
export const fetcher: (url: string) => Promise<any>;

<!-- Lightweight Charts v5 API (verified) -->
createChart(container, opts) -> IChartApi
chart.addSeries(LineSeries, opts) / chart.addSeries(AreaSeries, opts)
series.setData(points) / series.update(point) ; chart.remove()
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: MainChart component (FE-09) + test</name>
  <files>frontend/__tests__/MainChart.test.tsx, frontend/src/components/MainChart.tsx</files>
  <read_first>
    - frontend/src/components/SparklineChart.tsx (exact instance lifecycle to copy: createChart in useEffect, addSeries(LineSeries), tickCountRef monotonic counter, chart.remove() cleanup)
    - frontend/__tests__/SparklineChart.test.tsx (exact jest.mock('lightweight-charts') pattern, usePriceStore.setState reset in beforeEach, price dispatch via act())
    - frontend/src/stores/priceStore.ts (useTicker selector signature)
    - frontend/__mocks__/lightweightChartsStub.js (already configured mock — tests override with jest.mock per file)
  </read_first>
  <behavior>
    - Test: rendering MainChart calls createChart exactly once
    - Test: dispatching a price update for the ticker via usePriceStore.setState calls series.update with a monotonic time counter and the new price
    - Test: re-rendering with a different ticker prop calls series.setData([]) (resets the line)
  </behavior>
  <action>
    Write frontend/__tests__/MainChart.test.tsx FIRST (RED), copying the jest.mock('lightweight-charts') and store-reset setup from SparklineChart.test.tsx. The mock's addSeries must return an object exposing update, setData, and applyOptions jest.fn()s. Then implement frontend/src/components/MainChart.tsx as a default-exported component taking `{ ticker: string }`. Follow SparklineChart.tsx lifecycle with these differences: use `autoSize: true` in createChart options (instead of explicit width/height); show grid, rightPriceScale, and timeScale with border color `#30363d` and textColor `#8b949e` (do not hide them as sparklines do); use line color `#209dd7` (terminal-blue) lineWidth 2 via `addSeries(LineSeries, ...)`. Use `useTicker(ticker)` for live updates and append with `series.update({ time: tickCountRef.current as UTCTimestamp, value: priceUpdate.price })`, incrementing tickCountRef each tick. Add a SEPARATE `useEffect` with dependency `[ticker]` that calls `seriesRef.current?.setData([])` and resets `tickCountRef.current = 0` (Pitfall 1 — prevents discontinuous line on ticker switch). Render `<div ref={containerRef} style={{ width: '100%', height: '240px' }} />`. Never call createChart at module scope (SSR guard). Display the ticker in a title/header styled with accent `#ecad0a` per UI-SPEC (the selected-ticker emphasis).
  </action>
  <verify>
    <automated>cd frontend && npm test -- --testPathPattern=MainChart --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/MainChart.tsx contains `createChart` and `addSeries(LineSeries`
    - frontend/src/components/MainChart.tsx contains `useTicker(`
    - frontend/src/components/MainChart.tsx contains `setData([])` inside a `[ticker]` effect
    - frontend/src/components/MainChart.tsx contains `chart.remove()` in cleanup
    - frontend/__tests__/MainChart.test.tsx asserts createChart called once and setData([]) called on ticker change
    - `npm test -- --testPathPattern=MainChart --watchAll=false` exits 0
  </acceptance_criteria>
  <done>MainChart renders a live blue line for the selected ticker, resets cleanly on ticker change, and its tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PnLChart component (FE-11) + test</name>
  <files>frontend/__tests__/PnLChart.test.tsx, frontend/src/components/PnLChart.tsx</files>
  <read_first>
    - frontend/src/components/SparklineChart.tsx (chart instance lifecycle to copy)
    - frontend/src/components/Header.tsx (useSWR + fetcher usage pattern, refreshInterval option)
    - frontend/__tests__/SparklineChart.test.tsx (jest.mock('lightweight-charts') pattern with AreaSeries sentinel)
    - frontend/src/lib/fetcher.ts (fetcher signature)
    - frontend/src/types/market.ts (PortfolioHistoryResponse — added in Plan 01)
  </read_first>
  <behavior>
    - Test: rendering PnLChart calls createChart once and addSeries with the AreaSeries sentinel
    - Test: when useSWR returns snapshot data, series.setData is called with points whose time is the array index+1 and value is total_value
    - Test: empty/no-data state does not throw and does not call setData with points
  </behavior>
  <action>
    Write frontend/__tests__/PnLChart.test.tsx FIRST (RED). Mock 'lightweight-charts' (addSeries returns { setData, update, applyOptions }) and mock 'swr' default export to return `{ data: { snapshots: [...] } }` for the data-load test. Then implement frontend/src/components/PnLChart.tsx (default export, no props). Use `useSWR<PortfolioHistoryResponse>('/api/portfolio/history', fetcher, { refreshInterval: 30_000 })` — exact key string, no trailing slash. Create the chart in a mount `useEffect` (like SparklineChart) using `autoSize: true`, textColor `#8b949e`, grid color `#30363d`; add series via `addSeries(AreaSeries, { lineColor: '#209dd7', topColor: 'rgba(34, 197, 94, 0.4)', bottomColor: 'rgba(34, 197, 94, 0.0)', lineWidth: 2 })`. In a `useEffect` watching `data`, when `data?.snapshots?.length`, map snapshots to `{ time: (i + 1) as UTCTimestamp, value: s.total_value }` and call `seriesRef.current.setData(points)` (use array index for time per Pitfall 4 — do NOT parse recorded_at). Guard `if (!data?.snapshots?.length || !seriesRef.current) return`. When no snapshots, render the empty-state copy `No portfolio history yet.` from UI-SPEC. Clean up with `chart.remove()`. Render `<div ref={containerRef} style={{ width: '100%', height: '160px' }} />`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --testPathPattern=PnLChart --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/PnLChart.tsx contains `addSeries(AreaSeries`
    - frontend/src/components/PnLChart.tsx contains `'/api/portfolio/history'` and `refreshInterval: 30_000` (or `30000`)
    - frontend/src/components/PnLChart.tsx contains `setData(` and uses `(i + 1)` style index time (not Date parsing of recorded_at)
    - frontend/src/components/PnLChart.tsx contains the string `No portfolio history yet.`
    - frontend/__tests__/PnLChart.test.tsx asserts createChart called and setData called when data arrives
    - `npm test -- --testPathPattern=PnLChart --watchAll=false` exits 0
  </acceptance_criteria>
  <done>PnLChart renders an area chart of total portfolio value, polls every 30s, shows an empty state, and its tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client (chart data) | Chart values come from SSE store and /api/portfolio/history — app-owned numeric data |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-CHART | Tampering | chart point values | accept | Chart values are numeric (price, total_value) from the app's own store/API; no string rendering, no injection surface |
| T-4-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — lightweight-charts and swr already installed and approved (RESEARCH Package Legitimacy Audit) |
</threat_model>

<verification>
- `cd frontend && npm test -- --testPathPattern=MainChart --watchAll=false` passes
- `cd frontend && npm test -- --testPathPattern=PnLChart --watchAll=false` passes
</verification>

<success_criteria>
- MainChart renders live line for selected ticker, resets on ticker change (FE-09)
- PnLChart renders area chart from history with 30s poll and empty state (FE-11)
</success_criteria>

<output>
Create `.planning/phases/04-frontend-portfolio-trading/04-02-SUMMARY.md` when done
</output>
