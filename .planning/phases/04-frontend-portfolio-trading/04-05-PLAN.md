---
phase: 04-frontend-portfolio-trading
plan: 05
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/__tests__/ChatPanel.test.tsx
  - frontend/src/components/ChatPanel.tsx
autonomous: true
requirements: [FE-14, FE-15]
must_haves:
  truths:
    - "The chat panel loads the last 20 messages from GET /api/chat/ on mount and renders the conversation"
    - "Sending a message shows a loading indicator until the POST /api/chat/ response arrives, then appends the assistant reply"
    - "AI-executed trades and watchlist changes render as distinct badges below the assistant message"
    - "Chat message text is rendered safely (no HTML injection)"
  artifacts:
    - path: "frontend/src/components/ChatPanel.tsx"
      provides: "Collapsible chat panel: history load, send + loading, action badges"
      contains: "/api/chat/"
      min_lines: 50
  key_links:
    - from: "frontend/src/components/ChatPanel.tsx"
      to: "GET /api/chat/"
      via: "useSWR history load on mount"
      pattern: "useSWR.*api/chat"
    - from: "frontend/src/components/ChatPanel.tsx"
      to: "POST /api/chat/"
      via: "fetch on send + mutateHistory"
      pattern: "method: 'POST'"
---

<objective>
Build the AI chat panel (FE-14, FE-15): message input, scrolling conversation history loaded from GET /api/chat/, a loading indicator during POST /api/chat/, and inline badges below assistant messages for AI-executed trades and watchlist changes.

Purpose: This is the AI copilot surface — the most integration-heavy component (history load, send flow, badges, auto-scroll, XSS safety). It gets its own plan.
Output: ChatPanel.tsx and its Wave 0 test file (RED-first).
</objective>

<execution_context>
@/Users/maoqin/learning/ai-coder/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maoqin/learning/ai-coder/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-frontend-portfolio-trading/04-RESEARCH.md
@.planning/phases/04-frontend-portfolio-trading/04-PATTERNS.md
@.planning/phases/04-frontend-portfolio-trading/04-UI-SPEC.md

<interfaces>
<!-- From frontend/src/types/market.ts (added in Plan 01) -->
export interface ChatMessage { role: 'user' | 'assistant'; content: string; actions: { trades: TradeOutcome[]; watchlist_changes: WatchlistOutcome[] } | null; created_at: string; }
export interface ChatHistoryResponse { messages: ChatMessage[]; }
export interface ChatPostResponse { message: string; trades: TradeOutcome[]; watchlist_changes: WatchlistOutcome[]; }
export interface TradeOutcome { status: 'executed'|'failed'; ticker: string; side?: string; quantity?: number; price?: number; trade_id?: string; error?: string; }
export interface WatchlistOutcome { status: string; ticker: string; action: string; }

<!-- Backend (note trailing slash — FastAPI router convention, RESEARCH Pitfall 7 / A2) -->
GET  /api/chat/  -> { messages: ChatMessage[] }   (added in Plan 01)
POST /api/chat/  -> { message, trades[], watchlist_changes[] }
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: ChatPanel component (FE-14, FE-15) + test</name>
  <files>frontend/__tests__/ChatPanel.test.tsx, frontend/src/components/ChatPanel.tsx</files>
  <read_first>
    - frontend/src/components/WatchlistPanel.tsx (panel container + SWR pattern)
    - frontend/__tests__/WatchlistRow.test.tsx (render + event test patterns)
    - .planning/phases/04-frontend-portfolio-trading/04-PATTERNS.md (ChatPanel section — history fetch with trailing slash, collapse, auto-scroll, submit+revalidate, action badge JSX, XSS note)
    - .planning/phases/04-frontend-portfolio-trading/04-UI-SPEC.md (chat copy: input placeholder, empty state, loading label, badge text formats; badge colors: buy=accent #ecad0a border, sell=red #ef4444 border, watchlist=muted #8b949e border; Send button purple #753991)
  </read_first>
  <behavior>
    - Test (FE-14): on mount, history is loaded via GET /api/chat/ and existing messages render
    - Test (FE-14): while a POST /api/chat/ is in flight, a loading indicator is visible; it disappears after the response resolves
    - Test (FE-15): an assistant message with actions.trades renders a badge whose text reflects the trade (e.g. side/qty/ticker/price)
    - Test (FE-15): an assistant message with actions.watchlist_changes renders a watchlist badge
    - Test (T-4-02 security): message content is rendered as a React text child (no dangerouslySetInnerHTML anywhere in the component)
  </behavior>
  <action>
    Write frontend/__tests__/ChatPanel.test.tsx FIRST (RED). Mock 'swr' default export to return `{ data: { messages: [...] }, mutate: jest.fn() }`; mock global.fetch for the send path. Then implement frontend/src/components/ChatPanel.tsx as default export taking `{ open: boolean; onToggle: () => void; onNewTrade?: () => void }`. Load history: `const { data: history, mutate: mutateHistory } = useSWR<ChatHistoryResponse>('/api/chat/', fetcher)` — TRAILING SLASH required (matches FastAPI router; without it the request 404s/307s, RESEARCH Pitfall 7). Local state: `input` (string), `loading` (boolean). Render a header with a collapse toggle button (text chevron `›` when open / `‹` when closed — do NOT add lucide-react) calling onToggle. Scrollable message list mapping `history?.messages`; each message is a bubble (`bg-terminal-surface` for the prose, user vs assistant alignment). Render message text as `{msg.content}` — NEVER dangerouslySetInnerHTML (XSS T-4-02). Below each assistant bubble whose `actions` is non-null, render badges: for each `actions.trades` entry a pill `Bought {qty} {ticker} @ {price}` / `Sold {qty} {ticker} @ {price}` (buy → accent #ecad0a border, sell → red #ef4444 border), and for each `actions.watchlist_changes` entry a pill `Added {ticker}` / `Removed {ticker}` (muted #8b949e border). Badge text is built ONLY from known fields (ticker/qty/price/action) — never raw LLM output (T-4-04). Empty state when no messages: `Ask FinAlly to analyze your portfolio, suggest trades, or manage your watchlist.` Input row: text input with placeholder `Ask FinAlly about your portfolio…`, a purple (#753991) `Send` button; Enter or click submits. handleSubmit: set loading, `fetch('/api/chat/', { method: 'POST', headers: {'Content-Type':'application/json'}, body: JSON.stringify({ message: input }) })`, await json, `await mutateHistory()` to refresh from server, and if `data.trades?.length || data.watchlist_changes?.length` call `onNewTrade?.()` so the parent revalidates /api/portfolio; finally set loading false and clear input. While loading, show the loading indicator (animated dots or `Thinking…` label per UI-SPEC). Auto-scroll: `const messagesEndRef = useRef<HTMLDivElement>(null)` placed after the list, and `useEffect(() => { messagesEndRef.current?.scrollIntoView({ behavior: 'smooth' }); }, [history?.messages?.length, loading])`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --testPathPattern=ChatPanel --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/ChatPanel.tsx contains `useSWR` with `'/api/chat/'` (trailing slash)
    - frontend/src/components/ChatPanel.tsx contains `method: 'POST'` to `'/api/chat/'`
    - frontend/src/components/ChatPanel.tsx contains `{msg.content}` and does NOT contain `dangerouslySetInnerHTML`
    - frontend/src/components/ChatPanel.tsx renders trade and watchlist badges from `actions.trades` / `actions.watchlist_changes`
    - frontend/src/components/ChatPanel.tsx contains the empty-state and placeholder copy strings from UI-SPEC
    - frontend/src/components/ChatPanel.tsx contains `scrollIntoView` auto-scroll
    - frontend/__tests__/ChatPanel.test.tsx asserts: history renders on mount, loading indicator during POST, trade badge renders, watchlist badge renders, no dangerouslySetInnerHTML
    - `npm test -- --testPathPattern=ChatPanel --watchAll=false` exits 0
  </acceptance_criteria>
  <done>ChatPanel loads history, sends messages with a loading state, renders safe text and action badges, and auto-scrolls; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| LLM/server → client (chat content) | Assistant message text and action data render in the browser |
| client → POST /api/chat/ | User message text crosses to backend |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-02 | Spoofing/XSS | message bubble rendering | mitigate (HIGH — blocking) | Render `{msg.content}` as a React text child; React auto-escapes. ChatPanel MUST NOT contain `dangerouslySetInnerHTML` — asserted by unit test |
| T-4-04 | Information Disclosure | action badges | mitigate | Badge text constructed only from known structured fields (ticker, qty, price, action) — never raw LLM message string |
| T-4-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — collapse toggle uses text chevron, not lucide-react (RESEARCH: avoid the new install) |
</threat_model>

<verification>
- `cd frontend && npm test -- --testPathPattern=ChatPanel --watchAll=false` passes
- `grep -L dangerouslySetInnerHTML frontend/src/components/ChatPanel.tsx` confirms the string is absent
</verification>

<success_criteria>
- ChatPanel loads history, shows loading on send, renders action badges (FE-14, FE-15)
- No XSS surface — content rendered as escaped React text (T-4-02)
</success_criteria>

<output>
Create `.planning/phases/04-frontend-portfolio-trading/04-05-SUMMARY.md` when done
</output>
