---
phase: 04-frontend-portfolio-trading
plan: 03
type: execute
wave: 2
depends_on: ["04-01"]
files_modified:
  - frontend/__tests__/PortfolioHeatmap.test.tsx
  - frontend/src/components/PortfolioHeatmap.tsx
  - frontend/__tests__/PositionsTable.test.tsx
  - frontend/src/components/PositionsTable.tsx
autonomous: true
requirements: [FE-10, FE-12]
must_haves:
  truths:
    - "The portfolio heatmap renders one tile per position, sized by portfolio weight and colored green/red by P&L"
    - "When there are no positions, the heatmap shows the empty-state copy"
    - "The positions table shows ticker, qty, avg cost, current price, unrealized P&L, and % change, with the live current-price cell flashing on each SSE tick"
  artifacts:
    - path: "frontend/src/components/PortfolioHeatmap.tsx"
      provides: "CSS flexbox treemap of positions"
      contains: "widthPct"
      min_lines: 25
    - path: "frontend/src/components/PositionsTable.tsx"
      provides: "Positions table with live flashing current-price column"
      contains: "useTicker"
      min_lines: 30
  key_links:
    - from: "frontend/src/components/PortfolioHeatmap.tsx"
      to: "/api/portfolio"
      via: "useSWR"
      pattern: "api/portfolio"
    - from: "frontend/src/components/PositionsTable.tsx"
      to: "priceStore useTicker + flash classes"
      via: "useTicker(pos.ticker) + animate-flash-*"
      pattern: "animate-flash-"
---

<objective>
Build the portfolio data panels: PortfolioHeatmap (FE-10 — CSS flexbox treemap, no library) and PositionsTable (FE-12 — Bloomberg-style compact table with live flashing current-price column). Both read `GET /api/portfolio` via SWR.

Purpose: These two panels visualize current holdings. They are independent files sharing the same `/api/portfolio` SWR key, so they are planned together.
Output: PortfolioHeatmap.tsx, PositionsTable.tsx, and their Wave 0 test files (RED-first).
</objective>

<execution_context>
@/Users/maoqin/learning/ai-coder/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maoqin/learning/ai-coder/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/phases/04-frontend-portfolio-trading/04-RESEARCH.md
@.planning/phases/04-frontend-portfolio-trading/04-PATTERNS.md
@.planning/phases/04-frontend-portfolio-trading/04-UI-SPEC.md

<interfaces>
<!-- From frontend/src/types/market.ts -->
export interface Position { ticker: string; quantity: number; avg_cost: number; current_price: number; unrealized_pnl: number; pnl_pct: number; }
export interface PortfolioResponse { cash: number; total_value: number; positions: Position[]; }

<!-- From frontend/src/stores/priceStore.ts -->
export const useTicker = (ticker: string) => PriceUpdate | undefined;

<!-- Tailwind tokens (tailwind.config.js): text-terminal-up #22c55e, text-terminal-down #ef4444,
     bg-terminal-surface #1a1a2e, text-terminal-text #e6edf3, text-terminal-muted #8b949e,
     border-terminal-border #30363d ; flash classes: animate-flash-up / animate-flash-down -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: PortfolioHeatmap component (FE-10) + test</name>
  <files>frontend/__tests__/PortfolioHeatmap.test.tsx, frontend/src/components/PortfolioHeatmap.tsx</files>
  <read_first>
    - frontend/src/components/WatchlistPanel.tsx (SWR fetch pattern, empty-state pattern, panel container structure)
    - frontend/__tests__/WatchlistPanel.test.tsx (how SWR is mocked for a panel component)
    - .planning/phases/04-frontend-portfolio-trading/04-PATTERNS.md (PortfolioHeatmap section — exact tile JSX, width%/alpha math, color tokens)
    - .planning/phases/04-frontend-portfolio-trading/04-UI-SPEC.md (heatmap color intensity rules and empty-state copy)
  </read_first>
  <behavior>
    - Test: given two positions, the heatmap renders two tiles each showing the ticker symbol, dollar value, and P&L%
    - Test: each tile's width% is proportional to positionValue / total_value (assert the inline width style)
    - Test: with no positions, the empty-state text "No positions yet. Use the trade bar to buy shares." renders
  </behavior>
  <action>
    Write frontend/__tests__/PortfolioHeatmap.test.tsx FIRST (RED), mocking 'swr' default export to return `{ data: { cash, total_value, positions: [...] } }` (and a separate test returning empty positions). Then implement frontend/src/components/PortfolioHeatmap.tsx (default export, no props). Use `useSWR<PortfolioResponse>('/api/portfolio', fetcher)` — exact key string, no trailing slash (must match Header.tsx so the cache is shared). Empty state: when `!positions || positions.length === 0`, render the exact copy `No positions yet. Use the trade bar to buy shares.` in `text-terminal-muted text-xs`. Otherwise render a `flex flex-wrap gap-1 p-2 bg-terminal-surface rounded` container. For each position compute `posValue = pos.quantity * pos.current_price`, `widthPct = (posValue / total_value) * 100`, `alpha = Math.min(Math.abs(pos.pnl_pct) / 20, 1.0)`, and background `rgba(34, 197, 94, Math.max(alpha, 0.3))` for profit (pnl_pct > 0), `rgba(239, 68, 68, Math.max(alpha, 0.3))` for loss (pnl_pct < 0), else `#1a1a2e`. Apply `style={{ width: ${widthPct}%, minWidth: '64px', backgroundColor: bg }}` (the 64px min keeps small positions clickable — Pitfall: no min-width makes them invisible). Each tile shows the ticker (font-semibold), `$${posValue.toFixed(0)}` (tabular-nums), and `{pnl_pct > 0 ? '+' : ''}{pnl_pct.toFixed(2)}%` colored text-terminal-up/down. Tile text class `text-terminal-text`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --testPathPattern=PortfolioHeatmap --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/PortfolioHeatmap.tsx contains `useSWR` with `'/api/portfolio'`
    - frontend/src/components/PortfolioHeatmap.tsx contains `minWidth: '64px'` and a `width: ` percent style derived from total_value
    - frontend/src/components/PortfolioHeatmap.tsx contains the string `No positions yet. Use the trade bar to buy shares.`
    - frontend/src/components/PortfolioHeatmap.tsx contains `rgba(34, 197, 94` and `rgba(239, 68, 68`
    - frontend/__tests__/PortfolioHeatmap.test.tsx asserts tile count and empty-state text
    - `npm test -- --testPathPattern=PortfolioHeatmap --watchAll=false` exits 0
  </acceptance_criteria>
  <done>Heatmap renders weight-sized, P&L-colored tiles with a 64px floor and shows the empty state when no positions; tests pass.</done>
</task>

<task type="auto" tdd="true">
  <name>Task 2: PositionsTable component (FE-12) + test</name>
  <files>frontend/__tests__/PositionsTable.test.tsx, frontend/src/components/PositionsTable.tsx</files>
  <read_first>
    - frontend/src/components/WatchlistRow.tsx (exact flash lifecycle: priceRef, flashTimeoutRef, void cell.offsetWidth reflow, animate-flash-up/down add+remove after 500ms)
    - frontend/src/components/WatchlistPanel.tsx (compact table structure: thead/tbody, text-xs, border-collapse, header styling)
    - frontend/__tests__/WatchlistRow.test.tsx (jest.useFakeTimers, usePriceStore.setState reset, flash-class assertion pattern)
    - .planning/phases/04-frontend-portfolio-trading/04-UI-SPEC.md (positions table column headers and P&L color rules)
  </read_first>
  <behavior>
    - Test: given a mock portfolio with one position, all six columns render (ticker, qty, avg cost, current price, unrealized P&L, % change)
    - Test: when a price update with direction 'up' arrives for the held ticker, the current-price cell gains class animate-flash-up; after 500ms (fake timers) the class is removed
    - Test: empty portfolio renders the positions empty-state copy
  </behavior>
  <action>
    Write frontend/__tests__/PositionsTable.test.tsx FIRST (RED), mocking 'swr' to return `{ data: { cash, total_value, positions: [mockPosition] } }`, using jest.useFakeTimers() and resetting usePriceStore in beforeEach (copy WatchlistRow.test.tsx setup). Then implement frontend/src/components/PositionsTable.tsx with a default-exported `PositionsTable` (no props) that calls `useSWR<PortfolioResponse>('/api/portfolio', fetcher)` (exact key — shared cache) and renders a compact `<table className="w-full text-xs border-collapse">` with headers `Ticker · Qty · Avg Cost · Price · P&L · Change %` (text-terminal-muted, border-b border-terminal-border). Empty state when no positions: copy `No positions yet. Use the trade bar to buy shares.` Implement an inner `PositionsRow` component per position that: calls `useTicker(pos.ticker)`; holds `priceRef = useRef<HTMLTableCellElement>(null)` and `flashTimeoutRef`; in a `useEffect` keyed on `[priceUpdate?.direction, priceUpdate?.timestamp]` reuses the WatchlistRow flash lifecycle (clear prior timeout, remove flash classes, return early if direction is 'flat', `void cell.offsetWidth` reflow, add `animate-flash-up`/`animate-flash-down`, remove after 500ms via setTimeout, clear timeout in cleanup). Compute live values: `currentPrice = priceUpdate?.price ?? pos.current_price`, `liveUnrealizedPnl = (currentPrice - pos.avg_cost) * pos.quantity`, `livePnlPct = pos.avg_cost > 0 ? ((currentPrice - pos.avg_cost) / pos.avg_cost) * 100 : 0`. The current-price cell uses `ref={priceRef}`. P&L and Change% text colored text-terminal-up when >= 0, text-terminal-down when < 0, text-terminal-muted when exactly 0. All numeric cells use `tabular-nums`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --testPathPattern=PositionsTable --watchAll=false</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/PositionsTable.tsx contains `useTicker(` and `useSWR` with `'/api/portfolio'`
    - frontend/src/components/PositionsTable.tsx contains `animate-flash-up` and `animate-flash-down` and `void` reflow
    - frontend/src/components/PositionsTable.tsx renders all six column headers (Ticker, Qty, Avg Cost, Price, P&L, Change %)
    - frontend/src/components/PositionsTable.tsx contains `No positions yet.` empty-state copy
    - frontend/__tests__/PositionsTable.test.tsx asserts six columns render and the current-price cell flashes then clears after 500ms
    - `npm test -- --testPathPattern=PositionsTable --watchAll=false` exits 0
  </acceptance_criteria>
  <done>Positions table shows all six columns with live, flashing current-price cells and a proper empty state; tests pass.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| server → client (portfolio data) | Position fields from /api/portfolio rendered as React text children |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-RENDER | Spoofing/Injection | tile + table cell rendering | mitigate | All values rendered as React text children ({pos.ticker}, {value}) — auto-escaped; no dangerouslySetInnerHTML |
| T-4-NAN | Denial of Service | pnl_pct / division | mitigate | Guard `pos.avg_cost > 0` before dividing; total_value used for width% comes from backend (non-zero when positions exist) |
| T-4-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages — heatmap is hand-built CSS (CONTEXT D-05); swr already approved |
</threat_model>

<verification>
- `cd frontend && npm test -- --testPathPattern=PortfolioHeatmap --watchAll=false` passes
- `cd frontend && npm test -- --testPathPattern=PositionsTable --watchAll=false` passes
</verification>

<success_criteria>
- Heatmap renders weight-sized P&L-colored tiles + empty state (FE-10)
- Positions table shows all six columns with live flashing price (FE-12)
</success_criteria>

<output>
Create `.planning/phases/04-frontend-portfolio-trading/04-03-SUMMARY.md` when done
</output>
