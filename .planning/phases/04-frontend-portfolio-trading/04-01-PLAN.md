---
phase: 04-frontend-portfolio-trading
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/src/types/market.ts
  - backend/app/routes/chat.py
  - backend/tests/test_chat.py
autonomous: true
requirements: [FE-14, FE-15]
must_haves:
  truths:
    - "GET /api/chat/ returns the last 20 chat messages in ascending chronological order"
    - "Frontend has TypeScript types for portfolio history and chat messages that match backend response shapes"
  artifacts:
    - path: "frontend/src/types/market.ts"
      provides: "PortfolioHistoryResponse, ChatMessage, ChatHistoryResponse, ChatPostResponse, TradeOutcome, WatchlistOutcome, PortfolioSnapshot types"
      contains: "export interface ChatHistoryResponse"
    - path: "backend/app/routes/chat.py"
      provides: "GET /api/chat/ history handler inside create_chat_router"
      contains: "async def get_chat_history"
    - path: "backend/tests/test_chat.py"
      provides: "test_get_chat_history integration test"
      contains: "test_get_chat_history"
  key_links:
    - from: "backend/app/routes/chat.py"
      to: "chat_messages table"
      via: "SELECT role, content, actions, created_at"
      pattern: "FROM chat_messages"
---

<objective>
Establish the shared contracts that all Phase 4 component plans depend on: the new frontend TypeScript types for portfolio history and chat, and the new backend `GET /api/chat/` history endpoint with its test.

Purpose: This is the interface-first foundation plan. Every component plan in Wave 2 imports types from `market.ts`; the ChatPanel plan consumes `GET /api/chat/`. Defining these contracts first prevents downstream scavenger-hunt exploration.
Output: Extended `frontend/src/types/market.ts`, new GET handler in `backend/app/routes/chat.py`, and `test_get_chat_history` in `backend/tests/test_chat.py`.
</objective>

<execution_context>
@/Users/maoqin/learning/ai-coder/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maoqin/learning/ai-coder/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/04-frontend-portfolio-trading/04-RESEARCH.md
@.planning/phases/04-frontend-portfolio-trading/04-PATTERNS.md

<interfaces>
<!-- Current frontend types (preserve all of these) — from frontend/src/types/market.ts -->
Existing exports to keep intact: PriceUpdate, PriceMap, WatchlistEntry, WatchlistResponse, Position, PortfolioResponse, DEFAULT_TICKERS

<!-- Backend GET /api/portfolio/history response (verified portfolio.py) -->
{ "snapshots": [ { "total_value": number, "recorded_at": ISO-string } ] }

<!-- Backend POST /api/chat response (verified chat.py) -->
{ "message": string, "trades": TradeOutcome[], "watchlist_changes": WatchlistOutcome[] }

<!-- chat_messages table columns: role, content, actions (JSON string|null), created_at -->
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Add GET /api/chat/ history endpoint with test</name>
  <files>backend/app/routes/chat.py, backend/tests/test_chat.py</files>
  <read_first>
    - backend/app/routes/chat.py (see existing create_chat_router factory and the @router.post("/") handler; note json and get_conn are already imported)
    - backend/app/routes/portfolio.py (the @router.get("/") handler — copy its conn = get_conn(db_path) / try / finally: conn.close() pattern)
    - backend/tests/test_chat.py (existing TestChat class and how it uses the chat_client fixture)
    - backend/tests/conftest.py (chat_client fixture — already registers the chat router and seeds the app)
  </read_first>
  <behavior>
    - Test test_get_chat_history: GET /api/chat/ returns 200 and a JSON body with a "messages" key whose value is a list
    - When chat_messages has rows, each returned message has keys role, content, actions, created_at; messages are ordered ascending by created_at; at most 20 are returned
    - actions is a parsed JSON object (or null), never a raw JSON string
  </behavior>
  <action>
    Add a new async handler `get_chat_history` registered with `@router.get("/")` INSIDE the existing `create_chat_router(price_cache, db_path)` factory, alongside the current `@router.post("/")` handler. The path resolves to `/api/chat/` (trailing slash — FastAPI router convention, matches existing POST). Query: `SELECT role, content, actions, created_at FROM chat_messages WHERE user_id = 'default' ORDER BY created_at DESC LIMIT 20`, then reverse the rows so the response is ascending by created_at. For each row build a dict with keys `role`, `content`, `actions` (use `json.loads(row["actions"]) if row["actions"] else None`), and `created_at`. Return `{"messages": messages}`. Use `conn = get_conn(db_path)` with a `try/finally: conn.close()` block exactly like portfolio.py. Do not add new imports — `json` and `get_conn` already exist in chat.py. Then add `test_get_chat_history` to the existing TestChat class in backend/tests/test_chat.py using the `chat_client` fixture: assert status 200, assert "messages" in body, assert it is a list. If feasible without heavy setup, also seed two messages (one user, one assistant with a non-null actions JSON) before the GET and assert ascending order and that actions is a dict for the assistant message.
  </action>
  <verify>
    <automated>cd backend && uv run --extra dev pytest tests/test_chat.py::TestChat::test_get_chat_history -v</automated>
  </verify>
  <acceptance_criteria>
    - backend/app/routes/chat.py contains `async def get_chat_history`
    - backend/app/routes/chat.py contains `@router.get("/")` registered inside create_chat_router
    - backend/app/routes/chat.py contains `FROM chat_messages` and `ORDER BY created_at DESC` and `LIMIT 20`
    - backend/tests/test_chat.py contains `test_get_chat_history`
    - `uv run --extra dev pytest tests/test_chat.py::TestChat::test_get_chat_history -v` exits 0
    - GET /api/chat/ returns 200 with a JSON object containing a `messages` array
  </acceptance_criteria>
  <done>GET /api/chat/ returns the last 20 chat messages ascending by created_at with actions parsed to objects; the new test passes.</done>
</task>

<task type="auto">
  <name>Task 2: Add Phase 4 frontend types to market.ts</name>
  <files>frontend/src/types/market.ts</files>
  <read_first>
    - frontend/src/types/market.ts (full current file — all existing exports MUST be preserved)
    - .planning/phases/04-frontend-portfolio-trading/04-PATTERNS.md (the market.ts section listing the exact new interfaces to add)
  </read_first>
  <action>
    Append (do not remove or alter existing exports) these exported interfaces to frontend/src/types/market.ts:
    `PortfolioSnapshot` with fields `total_value: number` and `recorded_at: string`;
    `PortfolioHistoryResponse` with `snapshots: PortfolioSnapshot[]`;
    `TradeOutcome` with `status: 'executed' | 'failed'`, `ticker: string`, and optional `side?: string`, `quantity?: number`, `price?: number`, `trade_id?: string`, `error?: string`;
    `WatchlistOutcome` with `status: string`, `ticker: string`, `action: string`;
    `ChatMessage` with `role: 'user' | 'assistant'`, `content: string`, `actions: { trades: TradeOutcome[]; watchlist_changes: WatchlistOutcome[] } | null`, `created_at: string`;
    `ChatHistoryResponse` with `messages: ChatMessage[]`;
    `ChatPostResponse` with `message: string`, `trades: TradeOutcome[]`, `watchlist_changes: WatchlistOutcome[]`.
    Match the snake_case field convention used throughout the existing file (these mirror backend response shapes).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/types/market.ts contains `export interface PortfolioHistoryResponse`
    - frontend/src/types/market.ts contains `export interface ChatHistoryResponse`
    - frontend/src/types/market.ts contains `export interface ChatPostResponse`
    - frontend/src/types/market.ts contains `export interface TradeOutcome`
    - frontend/src/types/market.ts still contains `export interface PortfolioResponse` and `export const DEFAULT_TICKERS` (existing exports preserved)
    - `npx tsc --noEmit` exits 0
  </acceptance_criteria>
  <done>All seven new interfaces exported from market.ts; existing exports untouched; TypeScript compiles with no errors.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| client → GET /api/chat/ | Client reads conversation history; backend returns DB rows |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-4-SQL | Tampering | get_chat_history query | mitigate | Query uses a fixed string with a literal `user_id = 'default'` filter — no user-supplied input is interpolated into SQL |
| T-4-ID | Information Disclosure | actions field in response | accept | actions come from the app's own chat_messages rows (no cross-user data — single-user app); returned as parsed JSON of fields the app itself wrote |
| T-4-SC | Tampering | npm/pip/cargo installs | mitigate | No new packages introduced in this plan; nothing to install |
</threat_model>

<verification>
- `cd backend && uv run --extra dev pytest tests/test_chat.py -v` passes (new + existing chat tests)
- `cd frontend && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- GET /api/chat/ endpoint live and tested
- All Phase 4 frontend types exported and compiling
- No existing exports or tests broken
</success_criteria>

<output>
Create `.planning/phases/04-frontend-portfolio-trading/04-01-SUMMARY.md` when done
</output>
