---
phase: 03-frontend-foundation
plan: 01
type: execute
wave: 1
depends_on: []
files_modified:
  - frontend/package.json
  - frontend/next.config.js
  - frontend/tailwind.config.js
  - frontend/postcss.config.js
  - frontend/tsconfig.json
  - frontend/src/styles/globals.css
  - frontend/src/types/market.ts
  - frontend/jest.config.js
  - frontend/jest.setup.ts
  - frontend/__mocks__/nextFontMock.js
  - frontend/src/pages/_app.tsx
autonomous: false
requirements: [FE-01, FE-03]
user_setup: []

must_haves:
  truths:
    - "npm run build produces a static export in frontend/out/"
    - "Dark terminal theme colors are available as Tailwind utility classes (bg-terminal-bg, text-terminal-accent, etc.)"
    - "JetBrains Mono font loads via next/font/google and applies globally"
    - "npm test runs Jest with jsdom + canvas mock and exits 0 on an empty/placeholder suite"
    - "PriceUpdate TypeScript interface matches backend snake_case field names exactly"
  artifacts:
    - path: "frontend/next.config.js"
      provides: "Static export configuration"
      contains: "output: 'export'"
    - path: "frontend/tailwind.config.js"
      provides: "Dark terminal theme color tokens + flash animations"
      contains: "terminal"
    - path: "frontend/src/types/market.ts"
      provides: "PriceUpdate, PriceMap, WatchlistResponse, PortfolioResponse types"
      exports: ["PriceUpdate", "PriceMap", "WatchlistResponse", "PortfolioResponse", "DEFAULT_TICKERS"]
    - path: "frontend/jest.config.js"
      provides: "Jest config with next/jest + jsdom + canvas mock"
      contains: "jsdom"
    - path: "frontend/src/styles/globals.css"
      provides: "Tailwind directives + flash CSS classes"
      contains: ".flash-up"
  key_links:
    - from: "frontend/src/pages/_app.tsx"
      to: "frontend/src/styles/globals.css"
      via: "import"
      pattern: "import.*globals.css"
    - from: "frontend/tailwind.config.js"
      to: "frontend/src/components"
      via: "content glob"
      pattern: "components.*tsx"
---

<objective>
Scaffold the Next.js Pages Router project with TypeScript and static export, install the locked dependency stack, configure the dark terminal Tailwind theme, define the shared market data types that mirror the backend contract, and stand up the Jest test infrastructure (Wave 0 gaps from RESEARCH.md). This is the foundation every other Phase 3 plan builds on.

Purpose: FE-01 (static export project) and FE-03 (dark terminal theme) require a correctly-configured project before any component can be built. The test infrastructure must exist before downstream plans can add component tests (Nyquist rule).
Output: A buildable, testable frontend skeleton with theme tokens, types, and font wired in.
</objective>

<execution_context>
@/Users/maoqin/learning/ai-coder/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maoqin/learning/ai-coder/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/phases/03-frontend-foundation/03-CONTEXT.md
@.planning/phases/03-frontend-foundation/03-RESEARCH.md
@.planning/phases/03-frontend-foundation/03-PATTERNS.md
@.planning/phases/03-frontend-foundation/03-UI-SPEC.md

<interfaces>
<!-- Backend contract the frontend types must mirror. Source: backend/app/market/models.py to_dict() (lines 39-48), backend/app/routes/portfolio.py (lines 226-239), backend/app/routes/watchlist.py (lines 108-118). All field names are snake_case — DO NOT camelCase. -->

PriceUpdate (SSE event + watchlist price source):
  ticker: string
  price: number
  previous_price: number
  timestamp: number          (Unix seconds, float)
  change: number
  change_percent: number
  direction: 'up' | 'down' | 'flat'

SSE event.data is a JSON object keyed by ticker: Record<string, PriceUpdate>

WatchlistEntry (GET /api/watchlist → { tickers: WatchlistEntry[] }):
  ticker: string
  added_at: string           (ISO timestamp)
  price: number | null
  change_percent: number | null
  direction: 'up' | 'down' | 'flat' | null

Position + PortfolioResponse (GET /api/portfolio):
  Position: ticker, quantity, avg_cost, current_price, unrealized_pnl, pnl_pct (all number)
  PortfolioResponse: { cash: number, total_value: number, positions: Position[] }

DEFAULT_TICKERS (from backend/app/market/seed_prices.py):
  ['AAPL','GOOGL','MSFT','AMZN','TSLA','NVDA','META','JPM','V','NFLX']
</interfaces>
</context>

<tasks>

<task type="checkpoint:human-verify" gate="blocking-human">
  <name>Task 1: Verify npm package legitimacy before install</name>
  <read_first>
    - .planning/phases/03-frontend-foundation/03-RESEARCH.md (## Package Legitimacy Audit — packages tagged [ASSUMED] because slopcheck was unavailable)
  </read_first>
  <what-built>
    RESEARCH.md confirmed all packages exist on the npm registry via `npm view`, but slopcheck could not run, so download counts and ages are tagged [ASSUMED]. Per the package legitimacy gate, the following installs require human verification before running: next, react, react-dom, typescript, lightweight-charts, zustand, swr, tailwindcss@^3, postcss, autoprefixer, jest, jest-environment-jsdom, @testing-library/react, @testing-library/dom, @testing-library/jest-dom, ts-node, @types/jest, jest-canvas-mock.
  </what-built>
  <how-to-verify>
    1. Confirm each production package on its registry page: npmjs.com/package/next, npmjs.com/package/lightweight-charts, npmjs.com/package/zustand, npmjs.com/package/swr, npmjs.com/package/tailwindcss
    2. Confirm publisher matches the expected source repo (vercel/next.js, tradingview/lightweight-charts, pmndrs/zustand, vercel/swr, tailwindlabs/tailwindcss)
    3. Confirm dev/test packages: jest, @testing-library/react, jest-canvas-mock are the well-known maintainers (jestjs, testing-library, hustcc)
  </how-to-verify>
  <action>Block all subsequent install tasks until the user confirms the packages are legitimate. This checkpoint is never auto-approvable regardless of workflow.auto_advance. Addresses threat T-03-SC.</action>
  <verify>
    <automated>MISSING — human gate; no automated check. Approval recorded in SUMMARY.</automated>
  </verify>
  <resume-signal>Type "approved" to proceed with npm installs, or list packages to remove.</resume-signal>
  <done>User has confirmed package legitimacy; downstream install tasks may run.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Scaffold Next.js Pages Router project with static export and Tailwind v3</name>
  <files>frontend/package.json, frontend/next.config.js, frontend/tsconfig.json, frontend/tailwind.config.js, frontend/postcss.config.js, frontend/src/pages/_app.tsx, frontend/src/styles/globals.css</files>
  <read_first>
    - .planning/phases/03-frontend-foundation/03-RESEARCH.md (## Code Examples — exact scaffold command, Pitfall 6 next.config syntax, Pitfall 7 Tailwind v4-vs-v3 check)
    - .planning/phases/03-frontend-foundation/03-PATTERNS.md (frontend/next.config.js, frontend/tailwind.config.js, frontend/src/styles/globals.css, frontend/src/pages/_app.tsx sections — copy color tokens, keyframes, font setup verbatim)
    - .planning/phases/03-frontend-foundation/03-UI-SPEC.md (## Color — locked hex values; ## Typography — JetBrains Mono, two weights 400/600)
  </read_first>
  <action>
Scaffold with `npx create-next-app@latest frontend --ts --eslint --no-tailwind --src-dir --no-app --no-turbopack --no-react-compiler --import-alias "@/*" --use-npm` (per D-01 Pages Router, per RESEARCH Code Examples). Use `--no-tailwind` to avoid the Tailwind v4 scaffold trap (Pitfall 7), then install Tailwind v3 manually: `npm install -D tailwindcss@^3 postcss autoprefixer` and `npx tailwindcss init -p`. Install production deps: `npm install lightweight-charts zustand swr`.

Set `frontend/next.config.js` to CommonJS form (NOT next.config.ts — Pitfall 6) with `output: 'export'` and `images: { unoptimized: true }`, using `module.exports = nextConfig`.

Write `frontend/tailwind.config.js` (v3 syntax, NOT v4 @theme) with content globs `./src/pages/**/*.{js,ts,jsx,tsx}` and `./src/components/**/*.{js,ts,jsx,tsx}`. Add `theme.extend.fontFamily.mono = ['var(--font-mono)','ui-monospace','monospace']`. Add the full `terminal` color palette with these exact locked hex values (UI-SPEC ## Color, FE-03): bg `#0d1117`, surface `#1a1a2e`, border `#30363d`, text `#e6edf3`, muted `#8b949e`, accent `#ecad0a`, blue `#209dd7`, purple `#753991`, up `#22c55e`, down `#ef4444`, amber `#f59e0b`. Add `keyframes.flashUp`/`flashDown` and `animation['flash-up']`/`['flash-down']` per PATTERNS.

Write `frontend/src/styles/globals.css` with `@tailwind base; @tailwind components; @tailwind utilities;` plus `.flash-up` and `.flash-down` classes (green/red rgba background + `transition: background-color 500ms ease-out`) per PATTERNS / D-07.

Write `frontend/src/pages/_app.tsx` (per D-02 minimal _app): import `JetBrains_Mono` from `next/font/google` with `subsets:['latin']`, `weight:['400','600']`, `variable:'--font-mono'`, `display:'swap'`; import `@/styles/globals.css`; wrap `<Component>` in `<main className={`${jetbrainsMono.variable} font-mono`}>`.

Verify Pitfall 7: after install, `frontend/package.json` must show `tailwindcss` at `^3.x` and `frontend/tailwind.config.js` must exist (not empty). If create-next-app left a `frontend/src/app/` directory, delete it (App Router artifact — D-01 mandates Pages Router only).
  </action>
  <verify>
    <automated>cd frontend && grep -q "output: 'export'" next.config.js && grep -q "'#ecad0a'\|#ecad0a" tailwind.config.js && grep '"tailwindcss"' package.json | grep -q '\^3' && npm run build && test -d out && test -f out/index.html</automated>
  </verify>
  <acceptance_criteria>
    - frontend/next.config.js contains `output: 'export'` and uses `module.exports`
    - frontend/package.json `tailwindcss` dependency is `^3.x` (NOT 4.x)
    - frontend/tailwind.config.js contains the literal strings `#ecad0a`, `#209dd7`, `#753991`, `#0d1117`, `#1a1a2e`
    - frontend/src/styles/globals.css contains `.flash-up` and `.flash-down` with `transition: background-color`
    - frontend/src/pages/_app.tsx imports `JetBrains_Mono` and `@/styles/globals.css`
    - No frontend/src/app/ directory exists (Pages Router only)
    - `cd frontend && npm run build` exits 0 and produces frontend/out/index.html
  </acceptance_criteria>
  <done>Project scaffolds, builds to static out/, theme tokens and font are wired.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Define market types and Jest test infrastructure</name>
  <files>frontend/src/types/market.ts, frontend/jest.config.js, frontend/jest.setup.ts, frontend/__mocks__/nextFontMock.js</files>
  <read_first>
    - .planning/phases/03-frontend-foundation/03-PATTERNS.md (frontend/src/types/market.ts section — exact interface field names; frontend/__mocks__/nextFontMock.js section)
    - .planning/phases/03-frontend-foundation/03-RESEARCH.md (## Validation Architecture — jest framework, Wave 0 gaps, next/jest + canvas mock notes)
    - backend/app/market/models.py (to_dict method, lines 39-48 — confirm snake_case field names)
  </read_first>
  <action>
Install test deps (gated by Task 1 approval): `npm install -D jest jest-environment-jsdom @testing-library/react @testing-library/dom @testing-library/jest-dom ts-node @types/jest jest-canvas-mock`.

Write `frontend/src/types/market.ts` exporting (snake_case field names exactly, per PATTERNS and backend to_dict — DO NOT camelCase): `PriceUpdate` interface (ticker, price, previous_price, timestamp, change, change_percent, direction: 'up'|'down'|'flat'); `PriceMap = Record<string, PriceUpdate>`; `WatchlistEntry` (ticker, added_at, price: number|null, change_percent: number|null, direction: 'up'|'down'|'flat'|null); `WatchlistResponse = { tickers: WatchlistEntry[] }`; `Position` (ticker, quantity, avg_cost, current_price, unrealized_pnl, pnl_pct — all number); `PortfolioResponse = { cash: number; total_value: number; positions: Position[] }`; and `DEFAULT_TICKERS` const array of the 10 seed tickers (AAPL, GOOGL, MSFT, AMZN, TSLA, NVDA, META, JPM, V, NFLX).

Write `frontend/jest.config.js` using `next/jest` (`createJestConfig` from `next/jest`): set `testEnvironment: 'jsdom'`, `setupFilesAfterEnv: ['<rootDir>/jest.setup.ts']`, `moduleNameMapper` mapping `^@/(.*)$` → `<rootDir>/src/$1` and `next/font/google` → `<rootDir>/__mocks__/nextFontMock.js`.

Write `frontend/jest.setup.ts` with `import '@testing-library/jest-dom'` and `import 'jest-canvas-mock'` (the canvas mock lets Lightweight Charts' createChart run under jsdom).

Write `frontend/__mocks__/nextFontMock.js` exporting `JetBrains_Mono: () => ({ variable: '--font-mono', className: 'mock-font' })` per PATTERNS.

Add a `"test": "jest"` script to package.json scripts. Create a placeholder `frontend/__tests__/smoke.test.ts` with a single `expect(true).toBe(true)` so `npm test` runs green before downstream component tests exist (it will be replaced/augmented in later plans).
  </action>
  <verify>
    <automated>cd frontend && npx tsc --noEmit -p tsconfig.json && npm test -- --watchAll=false 2>&1 | grep -qE "Tests:.*passed|PASS"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/types/market.ts exports `PriceUpdate`, `PriceMap`, `WatchlistResponse`, `PortfolioResponse`, `DEFAULT_TICKERS`
    - frontend/src/types/market.ts contains `previous_price`, `change_percent`, `avg_cost`, `unrealized_pnl`, `pnl_pct` (snake_case — grep confirms)
    - frontend/jest.config.js maps `next/font/google` to the mock and uses jsdom
    - frontend/jest.setup.ts imports `jest-canvas-mock`
    - `cd frontend && npx tsc --noEmit` exits 0 (types compile)
    - `cd frontend && npm test -- --watchAll=false` exits 0
  </acceptance_criteria>
  <done>Types compile, Jest runs green with jsdom + canvas mock + font mock configured.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| npm registry → local build | Third-party packages installed into the project; supply-chain surface |
| build pipeline → static out/ | Next.js pre-renders pages at build time |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-SC | Tampering | npm installs (next, lightweight-charts, zustand, swr, tailwind, jest stack) | mitigate | Blocking human checkpoint (Task 1) verifying registry/publisher before install; all packages [ASSUMED] per RESEARCH audit |
| T-03-01 | Denial of Service | `next build` pre-render of EventSource-using pages | accept | No EventSource at module/render scope in this plan; pre-render safe. Enforced in Plan 02. |
</threat_model>

<verification>
- `cd frontend && npm run build` succeeds and emits out/index.html (FE-01)
- Tailwind config carries all locked hex tokens (FE-03)
- `cd frontend && npm test -- --watchAll=false` exits 0 (test infra ready for downstream plans)
- `cd frontend && npx tsc --noEmit` exits 0
</verification>

<success_criteria>
- Next.js Pages Router project builds to a static export in frontend/out/ (FE-01)
- Dark terminal theme tokens and JetBrains Mono font are wired and usable (FE-03)
- Shared market types mirror the backend snake_case contract
- Jest + jsdom + canvas mock + next/font mock are configured and green
</success_criteria>

<output>
Create `.planning/phases/03-frontend-foundation/03-01-SUMMARY.md` when done
</output>
