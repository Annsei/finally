---
phase: 03-frontend-foundation
plan: 03
type: execute
wave: 3
depends_on: ["03-01", "03-02"]
files_modified:
  - frontend/src/components/Header.tsx
  - frontend/src/lib/fetcher.ts
  - frontend/__tests__/Header.test.tsx
autonomous: true
requirements: [FE-02]
user_setup: []

must_haves:
  truths:
    - "Header shows portfolio total value and cash balance from GET /api/portfolio"
    - "Header shows a connection status dot colored green/amber/red driven by Zustand connectionStatus"
    - "Cash and portfolio numbers use tabular figures and do not cause layout shift on update"
  artifacts:
    - path: "frontend/src/components/Header.tsx"
      provides: "Header bar with portfolio value, cash, connection dot"
      exports: ["default"]
    - path: "frontend/src/lib/fetcher.ts"
      provides: "Shared SWR fetcher"
      exports: ["fetcher"]
  key_links:
    - from: "frontend/src/components/Header.tsx"
      to: "/api/portfolio"
      via: "useSWR"
      pattern: "useSWR.*api/portfolio"
    - from: "frontend/src/components/Header.tsx"
      to: "frontend/src/stores/priceStore.ts"
      via: "connectionStatus selector"
      pattern: "usePriceStore"
---

<objective>
Build the Header bar: live portfolio total value and cash balance fetched from /api/portfolio via SWR, plus a connection status dot whose color is driven by the Zustand connectionStatus atom.

Purpose: FE-02 requires the header to show portfolio total value, a connection status indicator (green/yellow/red dot), and cash balance.
Output: Header.tsx + a shared SWR fetcher, unit-tested for the three dot states and value rendering.
</objective>

<execution_context>
@/Users/maoqin/learning/ai-coder/finally/.claude/get-shit-done/workflows/execute-plan.md
@/Users/maoqin/learning/ai-coder/finally/.claude/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/phases/03-frontend-foundation/03-RESEARCH.md
@.planning/phases/03-frontend-foundation/03-PATTERNS.md
@.planning/phases/03-frontend-foundation/03-UI-SPEC.md

<interfaces>
<!-- From Plan 01: frontend/src/types/market.ts -->
PortfolioResponse = { cash: number; total_value: number; positions: Position[] }
<!-- GET /api/portfolio. Source: backend/app/routes/portfolio.py lines 236-239 -->

<!-- From Plan 02: frontend/src/stores/priceStore.ts -->
usePriceStore((s) => s.connectionStatus): 'connected' | 'reconnecting' | 'disconnected'

<!-- Dot color contract (UI-SPEC ## Color, FE-02) -->
connected    → green  bg-terminal-up   (#22c55e)
reconnecting → amber  bg-terminal-amber (#f59e0b)   (accent yellow stays reserved for row selection)
disconnected → red    bg-terminal-down (#ef4444)

<!-- Typography (UI-SPEC ## Typography) -->
Portfolio total value: 20px / weight 600 (display)
Cash balance: 14px / weight 400 (data). Both tabular-nums to prevent layout shift.
Labels 'Portfolio' / 'Cash': 12px / weight 600.
</interfaces>
</context>

<tasks>

<task type="auto" tdd="true">
  <name>Task 1: Implement shared fetcher and Header component</name>
  <files>frontend/src/lib/fetcher.ts, frontend/src/components/Header.tsx, frontend/__tests__/Header.test.tsx</files>
  <read_first>
    - .planning/phases/03-frontend-foundation/03-PATTERNS.md (frontend/src/components/Header.tsx section — DOT_COLORS map, SWR usage, single-atom connectionStatus selector; Shared Patterns ## SWR Fetcher)
    - .planning/phases/03-frontend-foundation/03-UI-SPEC.md (## Color connection-dot decision: amber #f59e0b for reconnecting; ## Typography display vs data sizes; ## Copywriting header labels Portfolio/Cash)
    - frontend/src/stores/priceStore.ts (connectionStatus atom — Plan 02)
    - frontend/src/types/market.ts (PortfolioResponse — Plan 01)
  </read_first>
  <behavior>
    - Test 1: With connectionStatus 'connected' in the store, the dot element has class bg-terminal-up
    - Test 2: With 'reconnecting', the dot has class bg-terminal-amber
    - Test 3: With 'disconnected', the dot has class bg-terminal-down
    - Test 4: When SWR returns { cash: 10000, total_value: 12345.67, positions: [] }, the rendered output contains '10,000' and '12,345.67'
    - Test 5: Before data loads (undefined), cash/value render a placeholder '—' without throwing
  </behavior>
  <action>
Create `frontend/src/lib/fetcher.ts` exporting `export const fetcher = (url: string) => fetch(url).then((r) => r.json())` (PATTERNS Shared Patterns).

Create `frontend/src/components/Header.tsx` per PATTERNS. Read `connectionStatus` via single-atom selector `usePriceStore((s) => s.connectionStatus)` (NOT an object selector — Pitfall 2). Fetch portfolio via `useSWR<PortfolioResponse>('/api/portfolio', fetcher, { refreshInterval: 5000 })`. Define `DOT_COLORS = { connected: 'bg-terminal-up', reconnecting: 'bg-terminal-amber', disconnected: 'bg-terminal-down' }` (amber for reconnecting per UI-SPEC decision so accent yellow stays reserved for selection). Render a `<header>` with terminal-surface bg and bottom border: a `FinAlly` brand label in terminal-accent; a `Cash` label + value; a `Portfolio` label + total_value; a connection dot `<div className={\`w-2 h-2 rounded-full ${DOT_COLORS[connectionStatus]}\`} title={connectionStatus} />`. Format numbers with `toLocaleString('en-US', { minimumFractionDigits: 2 })` and fall back to `'—'` when data is undefined. Apply `tabular-nums` to numeric spans and use the display size (text-xl/font-semibold) for portfolio total, data size (text-sm) for cash, label size (text-xs/font-semibold) for the Portfolio/Cash labels.

Write `frontend/__tests__/Header.test.tsx`: reset `usePriceStore` between tests and set connectionStatus via `usePriceStore.setState({ connectionStatus })`. Mock `global.fetch` to resolve the PortfolioResponse JSON (or mock the `swr` module). Render with `@testing-library/react`, assert dot class via the element's className and assert formatted numbers via `screen.getByText` / text content. Wrap async SWR rendering in `findBy*` or `waitFor`.
  </action>
  <verify>
    <automated>cd frontend && npm test -- --watchAll=false --testPathPattern=Header 2>&1 | grep -qE "PASS|Tests:.*passed"</automated>
  </verify>
  <acceptance_criteria>
    - frontend/src/components/Header.tsx contains `useSWR` against `/api/portfolio` and reads connectionStatus from usePriceStore
    - DOT_COLORS maps connected→bg-terminal-up, reconnecting→bg-terminal-amber, disconnected→bg-terminal-down
    - No object-literal selector against usePriceStore in the file
    - Header renders `—` placeholder when portfolio data is undefined (no crash)
    - `cd frontend && npm test -- --watchAll=false --testPathPattern=Header` exits 0 with all 5 behaviors passing
  </acceptance_criteria>
  <done>Header renders cash + portfolio value from SWR and a correctly-colored connection dot from Zustand; tests green.</done>
</task>

</tasks>

<threat_model>
## Trust Boundaries

| Boundary | Description |
|----------|-------------|
| /api/portfolio → Header | JSON response rendered into the DOM |

## STRIDE Threat Register

| Threat ID | Category | Component | Disposition | Mitigation Plan |
|-----------|----------|-----------|-------------|-----------------|
| T-03-H-XSS | Tampering | Header rendering of cash/total_value | mitigate | Values rendered as React text nodes (JSX escaping); numbers passed through toLocaleString; never dangerouslySetInnerHTML |
| T-03-H-NaN | Tampering | undefined/null portfolio fields | accept | Optional chaining + `—` fallback prevents render crash; same-origin trusted backend |
</threat_model>

<verification>
- `cd frontend && npm test -- --watchAll=false --testPathPattern=Header` exits 0
- `cd frontend && npm run build` succeeds
</verification>

<success_criteria>
- Header shows live portfolio total value, cash balance, and a green/amber/red connection dot (FE-02)
- Numbers use tabular figures; undefined data renders a safe placeholder
</success_criteria>

<output>
Create `.planning/phases/03-frontend-foundation/03-03-SUMMARY.md` when done
</output>
